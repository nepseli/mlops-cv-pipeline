# Local training environment (Python 3.10-3.12 recommended).
#
# Lower bounds, not exact pins: exact pins from one era stop resolving on newer
# Python versions. The one constraint that matters is the MLflow MAJOR version —
# it must match the server image in k8s/mlflow.yaml, or model registration fails.
ultralytics>=8.3
mlflow>=3.0,<4.0          # keep in lockstep with ghcr.io/mlflow/mlflow:v3.x
dvc[s3]>=3.67.1
onnx>=1.22.0
onnxruntime>=1.18
pyyaml>=6.0.3
